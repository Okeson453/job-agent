# cv_profiles/general/cv.pdf

<!-- This directory expects an actual `cv.pdf` — generate it (Word/PDF
export) from the data in profile/, experience/, skills/, and projects/,
filtered per metadata.yaml's `emphasis`. This scaffold only prepares the
source data; rendering the PDF is a separate step once the fields above
are filled. Delete this NOTE.md once cv.pdf exists. -->
