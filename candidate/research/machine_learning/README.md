# research/machine_learning/

<!-- Notes, findings, writeups — one .md per topic/experiment, named
descriptively. `experiments/` should log method + result even for
failed experiments (that's often the more interesting interview answer).
`reports/` is for polished, shareable writeups (link source docs from
evidence/reports/). -->
