# Crash (CrashWave) — Achievements

<!-- No live/production performance numbers in the README. The
"production acceptance criteria" list (zero TS errors, zero ESLint
errors/warnings, no uncontrolled `any`, no production console.*, no
unsanitized HTML injection, no secrets in source, accessibility +
security rules passing) is a stated *bar*, not a confirmed result —
the README itself says final sign-off is still pending. -->
TODO once the verification gates below are actually cleared.
