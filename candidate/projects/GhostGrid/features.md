# Ghost Grid — Key Features

- Full H_c scoring system (HMP + HLCP + MPP), ported intact from
  PHANTOM GRID despite everything around it being simplified
- CVD divergence engine
- Multi-layer exit warfare (4 layers)
- Nuclear portfolio guardian (atomic multi-position close)
- Hardcoded, load-time-only risk governor — no runtime override path
- Telegram control interface: alerts, kill switch (`/nuke`), daily report
- Crash recovery + independent watchdog thread
