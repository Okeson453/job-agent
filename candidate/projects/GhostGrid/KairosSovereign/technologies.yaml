temporal_schema: "TimescaleDB (hypertable + trigger enforcement + provenance hash)"
gap_handler: "Python + Kafka (heartbeat monitor, synthetic bar emitter)"
event_bus: "Apache Kafka + Avro (partitioned append-only, state versioning)"
stream_processing: "Apache Faust (bounded-staleness bar construction)"
timeseries_storage: "QuestDB / ClickHouse (bi-temporal index, AS OF joins)"
feature_dag: "Apache Arrow + custom (bounded-staleness, entropy compression gate)"
feature_store: "Feast / custom Delta Lake (PIT-enforced, Bayesian deprecation)"
frac_differentiation: "NumPy + custom (regime-anchored d, cosine taper)"
regime_detection: "hmmlearn + PyTorch (HMM + GMM + PELT + CUSUM + forecast)"
regime_vault: "PostgreSQL + pgvector (episode archive, cosine similarity)"
adversarial_stressor: "SciPy + PyTorch (L-BFGS-B optimization-based perturbation)"
red_team: "DEAP (evolutionary counter-strategy generator)"
model_parliament: "LightGBM + XGBoost + PyTorch (six ministers + EWC + timeout gate)"
epistemic_uq: "PyTorch + SHAP (MC Dropout + VAE + SHAP divergence)"
model_serving: "ONNX Runtime + TorchScript + Triton"
ntm_memory: "Redis Cluster + PyTorch (symbol-sharded, persisted memory matrix)"
execution_twin: "Rust custom engine (L2 replay, outage simulation, MEV model)"
ghost_grid_mesh: "Rust / C++ (broker fingerprinting, stealth fragmentation)"
risk_guardian: "Rust, zero-copy (inline synchronous, state versioning)"
omega_loop: "Python + SciPy (daily worst-case pre-computation)"
retraining: "Kubernetes + Airflow (drift-triggered, priority queue)"
experiment_tracking: "MLflow + DVC"
observability: "Prometheus + Grafana (five-dimension + complexity ratio monitor)"
alerting: "Telegram + PostgreSQL (structured incident log)"
infrastructure: "AWS GPU (priority, weekly/monthly) + bare-metal CPU (daily)"
