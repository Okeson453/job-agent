# CrashWave — see `../Crash/`

The GitHub repo you gave for **Crash**
(https://github.com/Okeson453/Crash) identifies itself, in its own
README and GitHub "About" text, as **CrashWave**. These appear to be
the same project. This folder has been emptied to avoid duplicate,
possibly-conflicting content — everything now lives in `../Crash/`.

If "CrashWave" and "Crash" are actually two different, separately
existing things, say so and point me at CrashWave's real repo and
I'll un-merge them.
